In this exercise we will learn how to **plot functions** and **convert data** to the right format to use it with `matplotlib`.

To get started open the notebook in this exercise folder with `jupyter notebook`.
